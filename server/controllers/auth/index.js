const registerUser = require("./registerUser");
const authenticateUser = require("./authenticateUser");
const logUserOut = require("./logUserOut");

module.exports = {
  registerUser,
  authenticateUser,
  logUserOut,
};
